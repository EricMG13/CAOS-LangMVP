```text
/make-plan Refine the implemented CAOS desktop production design on branch codex/caos-production-design without changing its approved information architecture, original dark-and-blue palette, contained paper report, authority model, accessibility guarantees, or explicit unavailable states. Preserve every Design Is principle currently scoring 3. Address only the remaining P2 items: reduce repeated navigation and dense control groups without slowing expert access; define task-oriented help before building it; consolidate packaged fonts and shared snapshot reads only where production profiling proves material benefit; and defer saved views/recent credits until their contracts exist. Keep mobile and tablet out of scope. Do not add dependencies, new design-system layers, client-derived attention scores, percentile calculations, claim coverage, admin governance, sensitivity controls, or any capability the server does not serve. Plan file-level changes, focused checks, rollback boundaries, and an approval gate before implementation.
```
