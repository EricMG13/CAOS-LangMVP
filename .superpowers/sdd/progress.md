# Enterprise-testing readiness execution

Base: `ba97a89899440532686b08050127d48db9a509b9`
Plan: `docs/superpowers/plans/2026-09-01-enterprise-testing-readiness-execution.md`

| Task | Status | Implementer | Reviewer | Evidence |
| --- | --- | --- | --- | --- |
| 1. Truthful green development baseline | complete | Codex implementer | Codex reviewer: approved | Baseline 10+10 preserved; audit 0 failures; ledger complete; Ruff clean; backend 119 passed; frontend 113 passed; YAML loaded; independent re-review approved |
| 2. Remove external filing acquisition dependencies | complete | Codex implementer | Codex reviewer: approved | CP-4 and CP-1C acquisition lanes removed; prohibition scans every tracked CAOS file; build `1912cb0`; 26 tests pass; bundle current; 26 modules/0 drift; independent re-review approved; report: `enterprise-task-2-report.md` |
| 3. Remove external font dependency | complete | Codex implementer | Codex reviewer: approved | Native CSS font stacks; recursive shipped-file guard; all-context journey guard; restricted offline build passed; frontend 116 passed; workbench DCL 48.9 ms/FCP 124 ms with zero Google font requests; implementation `92626bf`; correction `3806c72`; report: `enterprise-task-3-report.md` |
| 4. Close resource leaks | complete | Codex implementer | Sol/XHigh phase gate: approved | Full strict backend: 687 passed, 2 PostgreSQL skips, one third-party Starlette deprecation; zero first-party ResourceWarning, RuntimeWarning, or unraisable-warning failures; focused lifecycle 65 passed; Ruff/diff clean; implementation through `24016e4`; proof `ceee0b1`; gate reviewed `d48c7d8`; report: `enterprise-task-4-report.md` |
| 5. Provider authority and false-success removal | complete | Codex implementer | Independent security + truth agents: approved | Full backend 766 passed; 2 PostgreSQL tests BLOCKED EXTERNAL; security audit 50/42 routes, 0 failures; Ruff/ledger/diff clean; qualified Anthropic-only production, guarded host control, same-loop lifecycle, minimal worker secrets; live qualification BLOCKED EXTERNAL; `enterprise-task-5-report.md` |
| 6. Semantic execution + Distressed pathway | complete | Codex (WIP) + Claude Fable 5.1 (ER-G1) | adversarial review `enterprise-task-6-adversarial-review.md` (28 findings, decisions D1–D9 recorded) | Python 3.14.6; backend `945 passed, 2 skipped, 1 warning`; `run_sec_audit.py` 50/42 routes, 0 failures; ledger complete (45 routes, 232 files); `CORPUS_FULL=1` host control `34 passed` (every route, both depths, through ordinary `start_run`); Ruff clean; frontend lint/tsc/unit (116) /build pass; rebased onto `origin/main` (43 ahead, 0 behind) and re-gated: backend `945 passed, 2 skipped`, Ruff clean, audit 0 failures, ledger complete (242 files), corpus `34 passed`, workbench smoke and a11y (70 combinations) green against the host-control dev server; report: `enterprise-task-6-report.md` |
| 7. Deep Research pathway | complete (PR open) | Claude Fable 5.1 (ER-G2) | confidence review in the report (one recovery gap found and fixed) | Branch `claude/er-task-07-deep-research` from `main` `91d9038`; Python 3.14.6; backend `968 passed, 2 skipped`; Ruff clean; `run_sec_audit.py` 52/42 routes, 0 failures; ledger complete (47 routes, 242 files, 121 features); `CORPUS_FULL=1` host control `34 passed` incl. DEEP_RESEARCH full through the approval gate; frontend lint/tsc/unit/build exit 0; workbench smoke (live Deep Research journey) and a11y (70 combinations) green against the host-control dev server; C22 pack and live-model qualification BLOCKED EXTERNAL; `docs/DECISIONS.md` §14.16; report: `enterprise-task-7-report.md` |
| 8. Documents-only journey | complete (PR open) | Claude Fable 5.1 (ER-G3) | confidence review in the report (five findings surfaced by the gates, all fixed) | Branch `claude/er-task-08-documents-journey` from `main` `a29c9f9`; Python 3.14.6; backend `1004 passed, 2 skipped`; Ruff clean; `run_sec_audit.py` 54/43 routes, 0 failures; ledger complete (49 routes, 249 files, 122 features); `CORPUS_FULL=1` host control `34 passed`; frontend lint/tsc/unit (116)/build exit 0; workbench smoke (six packs through `POST /api/intake`, two through the drop zone, refusal, reader gate) and a11y (70 combinations) green against the host-control dev server; ETR-B01 closed; live-model qualification BLOCKED EXTERNAL; `docs/DECISIONS.md` §14.17; report: `enterprise-task-8-report.md` |
| 9. Source-complete modelling | complete (PR open) | Claude Fable 5.1 (ER-G4) | confidence review in the report (one lineage-oracle bypass found and fixed; the rest verified or by design) | Branch `claude/enterprise-readiness-model-cccaaa` from `main` `c70902f`; Python 3.14.6 (`caos/server/.venv314` from the hashed lock); backend `1031 passed, 2 skipped` (baseline 1004 + 27 new); Ruff clean; `run_sec_audit.py` 54/43 routes, 0 failures; ledger complete (49 routes, 249 files, 122 features); `CORPUS_FULL=1` host control `34 passed` with lineage and model-effect assertions on every route and depth; frontend lint/tsc/unit (116)/build exit 0; workbench smoke and a11y (70 combinations, 0 violations, fresh server on :8766) green against the host-control dev server; ETR-B12 closed under host control; licensed market marks and live-model qualification BLOCKED EXTERNAL; `docs/DECISIONS.md` §14.18; report: `enterprise-task-9-report.md` |
| 10. Opinion, publication, reconstruction | complete (PR open) | Claude Fable 5.1 (ER-G5) | confidence review in the report (twelve doubts investigated; the security audit caught one real escalation in the members route and the inspection found eight PDF defects, all fixed) | Branch `claude/enterprise-report-signing-filing-e14fb4` from `main` `eea2b29`; Python 3.14.6 (`caos/server/.venv314` from the hashed lock); backend `1096 passed, 2 skipped` (baseline 1005 + corpus subset 26 + 58 new: audit chain 11, publication 30, audit package 10, cross-format goldens 7); Ruff clean; `run_sec_audit.py` 59/48 routes, 0 failures; ledger complete (54 routes, 250 files, 128 features); `CORPUS_FULL=1` host control `34 passed`; frontend lint/tsc/unit (123)/build exit 0; workbench smoke (sign-off, worker-published freeze, separation of duties, receipt) and a11y (70 combinations, 0 violations, fresh server) green against the host-control server; five invariant mutations each red; G7/G8 closed under host control; blind rubric review, live-model qualification and the PostgreSQL DDL exercise BLOCKED EXTERNAL or open; `docs/DECISIONS.md` §14.19; draft PR https://github.com/EricMG13/CAOS-LangMVP/pull/51; report: `enterprise-task-10-report.md`. CI follow-up 2026-09-03: the first CI run paginated the PDF goldens differently (host fonts) and the inspection of the regenerated long-text state found a paragraph-split defect in the Task 10 renderer (overprinted, blank and heading-only pages); fixed by vendoring DejaVu with digest verification, a hermetic fontconfig, pinned hinting and line heights, pre-wrapped prose and a bisected page split; goldens regenerated after every page (60) and sheet was inspected; backend `1098 passed, 2 skipped`; Ruff, audit, ledger, frontend lint/tsc/unit, smoke green; the CI browser job's focus timeout at `workbench-smoke.mjs:1251` (unchanged step, passes locally and on main) is instrumented and open — see the report's CI follow-up section |
| 11. Corpus and live matrix | complete (PR open) | Claude Fable 5.1 (ER-G6) | confidence review in the report (twelve doubts investigated; six harness defects and four wrong expectations fixed before the gates; two real-bytes product findings recorded) | Branch `claude/enterprise-corpus-qualification-636047` from `main` `12e5d04`; Python 3.14.6 (`caos/server/.venv314`); versioned manifests and attested answer keys for C01–C22 under `caos/tests/corpus/packs/` (Carnival shared by C01/C17–C20, C02–C16 synthetic and digest-pinned, C20–C22 external and unpinned); harness `caos/tests/corpus/qualify.py` (plan/cell/matrix/verdict/pin) with `scoring.py` and the answer-keyed host-control provider; backend `1116 passed, 3 skipped`; harness module `18 passed`; `CORPUS_FULL=1` host control `35 passed`; Carnival host-control matrix 32 passed, 5 blocked external (C20/C21/C22: `ANSWER_KEY_UNSIGNED`), verdict `ORCHESTRATION_PROOF_INCOMPLETE`, exit 2; Full Credit, Earnings Update and Covenant & Refinancing proven at both depths under host control, Relative Value, Distressed and Deep Research blocked by their external packs; Ruff clean; `run_sec_audit.py` 59/48 routes, 0 failures; ledger complete (54 routes, 283 files, 131 features); `docs/QUALITY_QUALIFICATION.csv` maps MOD-001–025; protected workflow `.github/workflows/enterprise-qualification.yml` dispatchable; `docs/DECISIONS.md` §14.20; live qualification, analyst-approved keys, licensed marks (C20), the Lumen pack (C21) and the research pack (C22) BLOCKED EXTERNAL; two findings on real bytes (intake `INTAKE_ISSUER_AMBIGUOUS` on the whole Carnival pack; formula cells import blank); draft PR https://github.com/EricMG13/CAOS-LangMVP/pull/52; report: `enterprise-task-11-report.md` |
| 12a. Database truth, simulations, single instance, backup | complete (PR open) | Claude Fable 5.1 (ER-G7) | confidence review in the report (eleven doubts investigated; the PostgreSQL target found seven store defects and the simulations found five lifecycle defects, all fixed before the gates) | Branch `claude/er-task-12a-database-truth` from `main` `ca6c33e`; Python 3.14.6 (`caos/server/.venv314`); two-connection PostgreSQL proof `caos/tests/test_postgres_races.py` (27 tests) in CI job `postgres` against the pinned container and locally against `caos-qa-pg` (`76 passed` with `test_single_instance.py`, no skips); SIM-001–SIM-030 retained in `docs/SIMULATION_LEDGER.csv` (pinned by `test_simulation_ledger.py`; new simulations in `spec/test_simulations_spec.py`, 25 tests); exclusive checkpoint-location lock (`caos/instance_lock.py`) proven by a second process and by two `dev.py` processes; Compose `deploy.replicas: 1` for app and worker, `caos/deploy/ENVIRONMENT_MANIFEST.md`; restore-drill F1–F3 repaired (`backup.sh` app-mount vault discovery and paused-writer snapshot point, `restore_drill.sh` full startup schema) with regression tests in `test_deploy_topology.py`; enterprise-image drill on images built from this tree under host control: backup under active writes in ~1 s with 0 failed requests, restore drill passed, 198 restored sources byte-verified and checkpoint store intact, second instance refused `INSTANCE_ALREADY_RUNNING`, parked run resumed after SIGKILL restart, full-depth run killed mid-execution recovered with every module once, dead worker claim requeued, `down -v` reset to an empty environment with all 19 startup tables; backend `1162 passed, 4 skipped` (three corpus tests skip because the Carnival corpus is not fetched in this worktree, one harness cell is nightly-only; no ResourceWarning) (a first run found one service-level filing test whose approver lacked stored standing — the new commit-time recheck refused it; the test now provisions the approver like its neighbours); Ruff clean; `run_sec_audit.py` 59/48 routes, 0 failures; ledger complete (54 routes, 342 files, 133 features, after the CI follow-up added F-OPS-13 and F-QUAL-04 for the two new product files the gate found untracked-then-tracked); frontend untouched; `docs/DECISIONS.md` §14.21; `SPEC_RECONCILIATION.md` D3 closed; ETR-B07 and ETR-B10 closed; soak and saturation faults candidate-only; live-model runs on the image BLOCKED EXTERNAL; draft PR https://github.com/EricMG13/CAOS-LangMVP/pull/54; report: `enterprise-task-12a-report.md` |
| 12b. Security, identity, browsers, accessibility, capacity | complete (PR open) | Claude Fable 5.1 (ER-G8) | confidence review in the report (fifteen doubts investigated; the new gates found four oracle errors in the audit draft, two harness defects in the capacity harness, and the three-engine journey found one Chromium focus race and two Safari-only focus defects in the product, all fixed) | Branch `claude/er-task-12b-perimeter` from `main` `e8ccc06`; Python 3.14.6 (`caos/server/.venv314`); `run_sec_audit.py` rewritten around OpenAPI discovery and a nine-actor matrix: `{'audited_routes': 59, 'matrix_cells': 507, 'cross_case_probes': 20, 'failures': 0}`; the AI pull-request review replaced by the recorded read-only review (`caos/scripts/recorded_review.py`, `security-review.yml`, `test_recorded_review.py` 7 tests) and every workflow pinned read-only/commit-pinned/digest-pinned/hashed (`test_workflow_security.py` 22 tests); scanner floors and image-digest binding with a CycloneDX SBOM (`caos/scripts/scan_floors.py`, `test_scan_floors.py` 5 tests, hashed `requirements-security.txt`, gitleaks by digest); `workbench-smoke.mjs` parameterised over Chromium/Firefox/WebKit with traces and screenshots on failure — final script and build pass in all three (chromium 138.7 s, firefox 147.4 s, webkit 160.6 s, no console errors) after fixing the `DraftDiscardDialog` restore-timer focus steal (the open Task 10 CI item) and two Safari-only opener defects (`AcceptDialog`, palette-initiated cancel); a11y sweep covers empty/populated/review/filed/loading/error/refusal states, 0 violations; `spec/test_limits_spec.py` (10) proves below/at/above for every ceiling; `qa/capacity.py limits` PASS ×7 + 1 edge-only NOT_EXERCISED at development scale, `profile`/`baseline`/`compare` candidate-only; `docs/PERIMETER_LEDGER.csv` maps IAM-001–020, SEC-001–030, WEB-001–015, PERF-001–015 (pinned by `test_perimeter_ledger.py`); backend `1211 passed, 4 skipped` with the QA PostgreSQL container; Ruff clean; ledger complete (54 routes, 134 features); frontend lint/tsc/unit (123)/build exit 0; `docs/DECISIONS.md` §14.22; ETR-B06 closed; SEC-023/024/025/028 and the live halves of IAM-016/WEB-013 BLOCKED EXTERNAL; draft PR https://github.com/EricMG13/CAOS-LangMVP/pull/55; report: `enterprise-task-12b-report.md` |
| 13. Candidate qualification | pending | — | — | — |
