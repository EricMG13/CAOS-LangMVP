# Probe captures (confidence review): the 720 rail strip with scrollbars un-hidden
# (Playwright hides scrollbars headless, so the evidence PNGs above cannot show the thumb),
# and the dirty Model state after the sign-off panel moved below the worksheet.
