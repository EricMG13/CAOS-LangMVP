# Task 10 controller visual review — in progress

Guidance: full project Impeccable skill, one context resolution for `caos/frontend/src/components`, complete product/audit references, actual CSS tokens and representative Analysis/chart components. Existing dark workspace/light paper identity is binding; no palette or theme change is requested. This record is a scoped screenshot review, not a claim of complete WCAG or performance qualification.

## First Run evidence

Root personally viewed all12 PNGs in `/private/tmp/caos-task10.j0GTQN/evidence/graph-retry-2/task10-graph-matrix/`: Full Credit, Earnings Update, Covenant & Refinancing, Relative Value, Distressed & Restructuring (screen/full), Deep Research/full, and Deep Research/screen refusal. The last is the existing disabled-choice/API422 contract, not a rendered twelfth DAG.

Root also viewed all3 PNGs in `/private/tmp/caos-task10.j0GTQN/evidence/run-graph-retry-1/`: branch/merge inspector at1440 and720, and malformed-graph inspector at720.

- Visible hierarchy, module labels, status words/glyphs, selected-node focus treatment and inline inspector are consistent with current workbench tokens.
- At720, controls and inspector stack; graph scrolling is local. The invalid graph visibly preserves the served node list and unavailable dependency explanation instead of guessed edges.
- Whole-page graph screenshots show only the current horizontal viewport, not every off-screen connection. Full DAG identity must be checked programmatically. Root found the matrix asserted only edge counts and assigned native SVG endpoint-to-node matching plus exact dependency-pair comparison; that strengthened rerun remains pending here.
- These captures do not establish all-engine Model/Analysis/Report visuals, loaded-chart axe, export fidelity or full Task10 acceptance. Those remain open.

No confirmed new product-layout defect from these screenshots. No blanket accessibility/performance score is assigned without the final measured checks.

## Actual accepted-v2 Chromium publication

Root read the complete successful `qualification-chromium-attempt-4/task10-qualification/chromium/qualification.json` under the same evidence root. This is actual disposable intake, accepted modules, signed model revision, v2 report, opinion, freeze and independent filing, not Task9's seeded accepted-artifact seam. Root independently matched the actual frozen PDF SHA-256 `f356a187c5315dcb54b7c964e175e6ae2898719dfe1c0ad66dd6ac8ea8ccd05b` and XLSX SHA-256 `f16d746c976b0f1de278420a00c107966383a293eb1c879726500a90c8f563ed` to the recorded receipt. This verifies artifact identity, not by itself offline audit validity.

Root personally viewed all46 PDF pages, rasterized at110dpi in `/private/tmp/caos-task10-root-pdf.lSOr3C/`. No page was skipped. All body text stays above the footer; wide raw tables retain repeated column headers and continue as vertical records. Long source/authority identities wrap or remain within page bounds. Revenue, EBITDA, CFO, adjustments, debt maturities, selected revision cash/FCF and score charts retain visible axes, fiscal periods, series keys and equivalent exact tables. All three debt keys remain visible at page38; its exact table continues at39. Scores use a zero baseline. Selected-revision ratio recipes explicitly fall back to exact tables at41–42 because their Decimal precision exceeds the shared export bound, rather than silently rounding into plots. The frozen pending-approval watermark and renderer/revision/source identities persist after detached filing as designed. No confirmed new PDF-layout defect.

Root viewed successful Report overview frames at1440/1024/720. The internal paper scroll means these whole-page frames show the opening section, not every exhibit. Root assigned content-focused chart/table, cash-flow worksheet and report exhibits in each engine/width; overview screenshots alone do not close those capability gates. Earlier failed-attempt3 Analysis and Model screenshots were reviewed diagnostically but are not the final successful visual set.

Native workbook sheet inspection, all-engine content frames, final measured accessibility/performance results and actual incremental-v2 publication remain pending in this record.

## Cross-engine follow-up and open paper-source finding

Root read Firefox attempt1 and WebKit attempt3 successful actual-v2 journey summaries with loaded Analysis-chart axe, real exports, independent approver receipts and no reported console errors. Root also read graph-retry3's complete served/displayed pair arrays: all11 supported DAGs match exactly using native SVG endpoint geometry, with the separate Deep Research/screen refusal retained. This closes the earlier count-only proof gap.

Root viewed WebKit attempt3's720 Analysis chart/table,720 Report chart/table, and720/1440 cash-flow worksheet captures. Analysis includes the complete chart, quarter labels, exact rows and governed links. Model shows actual settled Downside cash-flow values, reconciliation zeros, section/period controls and sticky labels; internal worksheet overflow is intentional.

Open finding assigned to Task10 owner: paper chart source labels/links inherit workspace colors because the shared paper overrides omit `.chart-exhibit-sources`. The focused Report720 screenshot visibly shows pale Sources/link text. Static WCAG luminance calculation from actual CSS tokens gives label2.315:1 and link1.949:1 against paper, below4.5:1. Existing paper-meta and paper-link both give approximately5.91:1. Owner must confirm with actual loaded-paper axe/computed styles, apply the shared paper-only correction, verify normal/hover/focus and all-engine loaded-paper contrast, then provide final frames. Existing `.deliverable-document :focus-visible` already uses paper-link. No unrelated theme change is needed.

## Native actual-v2 workbook review

Root copied the receipt-matched Chromium4 workbook to `/private/tmp/caos-task10-root-native.zdORyp/input/actual-v2.xlsx` and used installed LibreOffice26.2.5.2 with isolated `-env:UserInstallation=file:///private/tmp/caos-task10-root-native.zdORyp/profile`, `--headless`, and `pdf:calc_pdf_Export:{"SinglePageSheets":{"type":"boolean","value":"true"}}`. Initial sandbox invocation aborted134 with no output; the scoped approved native-app invocation succeeded0, with a nonblocking macOS task-policy warning. No global user profile was used. Poppler rasterization at90dpi succeeded0. Both native and raster processes finished before the owner resumed performance-sensitive tests.

Root personally viewed all30 native sheet images, not only chart sheets: `/private/tmp/caos-task10-root-native.zdORyp/pages/sheet-01.png` through30. Original frozen XLSX and copied input both still hash to `f16d746c976b0f1de278420a00c107966383a293eb1c879726500a90c8f563ed` after conversion.

- Sheets1–6: control/origin identity, Report narrative/index, credit driver, profile, complete112-row account register and4-row adjustment bridge. Dense registers are native worksheets; SinglePageSheets is a read-only inspection export, not a newly designed print layout. Existing ordinary-table formula-sanitizing apostrophes remain visible in LibreOffice for leading-minus text; the pre-existing `_safe_cell` behavior is retained. Chart equivalent tables instead use v4 explicit OOXML text cells/quotePrefix to preserve exact values.
- Sheets7–10: Revenue, EBITDA, CFO and adjustments plot all4ordered quarters with matching exact tables/helpers; lines are straight and the bar baseline is zero. Titles, units, categories and legends fit.
- Sheets11–15: full debt register; full-title FY2024_Q4 maturity chart with all3series and0/185/50; selected-revision output table; Base/Downside cash and FCF native charts. Cash/FCF raw equivalent rows match the reviewed PDF, while native helper numbers drive the same six values. Model output overview uses the existing numeric cell formatting; chart tables retain the unrounded exact strings.
- Sheets16–22: exact Net Leverage and Interest Coverage table-only precision fallbacks, covenant/RV tables, and68/72Composite Score native bars with full title and zero baseline.
- Sheets23–30: risks/monitoring/QA, complete3source/block Evidence Register, source disposition register, registered evidence inventory and renderer/draft/digest revision record.

No new native-chart/data-layout defect confirmed. This is actual accepted-v2 fixture export evidence on the final unchanged backend renderer, not live issuer analysis or an assertion that every historical table format was redesigned. The separate task9 positive/negative/mixed chart stress examples remain complementary evidence.

## Final-build paper follow-up

Root read final-harness `qualification-final-chromium/task10-qualification/chromium/qualification.json`: actual main journey passed, loaded Analysis and Report chart axe pass, paper source label `rgb(93,93,104)`, link and hover `rgb(47,84,201)`. Root viewed all3focused paper frames, plus1024Analysis and720cashflow: paper source contrast is visibly repaired. All-engine closure remains with final owner evidence.

New open responsive finding: final Chromium's1024 focused paper frame is only154px wide with publication controls open; the origin label clips and the source table is squeezed almost entirely outside its local viewport. Root traced the v2 three-column open layout to the900px breakpoint: at1024 the156px app rail plus content padding and190/300px minimum side panels leave too little paper width. Owner assigned actual open/closed geometry checks and reuse of the existing narrow report arrangement for the reachable v2 open state at the appropriate wider breakpoint. Closing controls for a reading screenshot alone does not repair the open-state layout. No runtime/framework or publication-authority change is requested.

## Definitive all-engine responsive closure

Root read all three successful JSON records under `.superpowers/sdd/evidence/task10/qualification-final/<engine>/`. Each actual accepted-v2 journey passes loaded Analysis and paper-chart axe, has no page errors, and measures **778px** paper with controls open at1024, versus **499.34px** (Firefox499.33px) with controls closed. Every engine records label `rgb(93,93,104)` and normal/hover link `rgb(47,84,201)`.

Root personally viewed each engine's complete focused1024 Revenue paper chart/table: all four FY2024 quarters, values100/110/120/130, USD MILLIONS, source/block link and locked-artifact label are visible and consistent. Root also reviewed Chromium and WebKit1024closed reading frames, Firefox1024Analysis chart/table and cash-flow worksheet. No new visual finding. Chromium's open full-page screenshot is16434px high and the image viewer downsamples it, so its legible-detail verification relies on the measured778px paper plus the complete focused exhibit rather than pretending that every text line was legible in the reduced full-page image. Earlier full export visual review remains separately scoped above.

Both root-found production CSS findings are closed on final all-engine evidence. The project Impeccable guidance led to reuse of existing paper contrast tokens and the existing narrow Report arrangement at1100px; no new palette, component framework or publication-authority change was needed.

## Full-application signed-stack closure

Root personally reviewed all three complete `analysis-task9-signed-stacks.png` captures: Chromium/Firefox under `evidence/task10/workbench-final/<engine>/`, and WebKit under `evidence/task10/failed-attempts/workbench-webkit-focus/`. These are explicitly route-controlled presentation fixtures on real accepted artifact/source identities, not analytical results.

Positive Q1/Q2 stacks preserve A=1 nearest zero and B=2 outward despite Q2's reversed input order; negative Q1/Q2 preserve A=-1 nearest zero and B=-2 outward. Mixed Q1/Q2/Q3 place each positive and negative segment consistently with global first-seen A/B/C ordering, and the exact tables retain all zero values and original category-local row order. All engines visually agree with Task9's previously reviewed browser/PDF/native-XLSX examples. No new visual finding.

WebKit's captured chart stage completed successfully before its later mouse-focus assumption failed. This establishes that visual subset only; it is not an overall smoke pass. The owner is rerunning WebKit after changing the test to explicit keyboard focus+Enter while retaining its focus assertions, with no chart or production change.

Final closure: root subsequently read all three successful `workbench-final/<engine>/workbench-report.json` records (Chromium162.394s, Firefox176.288s, WebKit179.895s), each with one loaded-chart axe check, all three signed-stack cases and no console errors. Root also personally viewed the successful WebKit retry's `workbench-final/webkit/analysis-task9-signed-stacks.png`; the same approved geometry, exact row order and source linkage are retained. The prior failed-stage provenance remains preserved rather than relabelled. Full-app all-engine visual gate is closed.
