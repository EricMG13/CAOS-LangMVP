# Analyst Workbench — independent whole-branch final review

## Verdict

- Specification: **PASS** against the complete user-approved Analyst Workbench capabilities plan, Tasks 0–10.
- Code quality: **Approved**.
- Actionable findings: **none**. No confirmed correctness, authority, compatibility, accessibility, or qualification blocker remains in the reviewed final branch.
- Readiness: ready for the controller's documentation-only closeout and user handoff. This assessment does not authorize merge, push, deployment, provider execution, or a claim of live analytical qualification.

## Exact scope and method

Reviewed baseline `31482a7912c9e58017c2784bf098a6868978cd01` through fixed implementation HEAD `715000ac30465aa2a06dc2283af9d0a60bed4894`, not `HEAD~1` or local main. The complete 27,777-line, 1,796,283-byte final review packet was read through EOF in bounded chunks; truncated ranges were reread. This covers all 25 branch commits, including production code, tests, scripts, textual fixtures, and qualification records. The complete tracked plan, repository instructions, global constraints, and final-review focus were read.

Used the code-reviewer skill and its applicable universal/TypeScript guidance for the read-only review. Read the Task 10 owner report and fresh findings, root confidence/accessibility/offline/visual records, and relevant Task 0–9 owner, failure, fix, and approval records. Previous approvals informed regression targets; they were not substituted for this whole-branch code/specification review.

Binary notices were inventoried separately: 214 PNGs, 7 PDFs, 6 XLSXs, and 5 ZIPs, all in Task 10 evidence. A binary notice is not visual inspection. Personal visual sampling is listed below. No production binary or vendored-methodology change was hidden in this inventory.

No source, tests, dependencies, branches, index, or HEAD were modified; no worker, browser journey, provider call, or broad test suite was started by this reviewer. The only write is this findings document. HEAD and clean tracked status were rechecked immediately before writing it.

## Full-plan and cross-workstream assessment

### Tasks 0–3: model contract, calculations, worksheet workspace

The served worksheet version is host-owned. New exact-pinned navigation is additive and does not replace raw lineage or alter historical payload/revision/export digests. Legacy NULL worksheet versions retain an explicit response-only navigation path tied to the exact displayed build, preview, or revision identity. Missing, ambiguous, withdrawn, wrong-block, or wrong-source authority cannot produce a fabricated exact citation.

Worksheet families and sections use served semantic structure, preserve formula/raw lineage inspection, support selection and collapse, and restrict assumption editing to READY forecast cells. Historical and forecast cash-flow predecessors remain distinct. Revision-head CAS and serialized calculation queues are retained. Minimum cash uses raw Decimal values over one exact ordered baseline horizon for every shock and fails closed on an unavailable shocked coordinate. The unchanged calculation implementation was inspected to confirm that an available endpoint cannot follow an unavailable baseline intermediate period; the horizon logic therefore does not silently omit a recoverable cash trough.

Case/layout/drawer replacement invalidates asynchronous model-evidence authority. Current-generation checks fence successful and failed responses, while actual connected openers and exact cited blocks govern focus. Unchanged API visibility helpers were checked: case authorization and case ownership are enforced before source/build data is returned. Tornado results remain bound to the current build, draft generation, queue authority, case, endpoint, output, and intensity.

### Task 4: served execution graph and stream lifecycle

The graph renders served stages and edge pairs, including branch/merge and skip edges, rather than constructing a sequential execution narrative. Accessible node buttons and inspectors preserve node identifiers and served status vocabulary. Malformed graph elements use the safe fallback consistently. Run-level pauses, errors, and actions remain outside node-specific invention; reconnecting freshness is separate from execution state.

Unchanged run refresh callers were checked for case/run/request fencing on success, error, and completion. Stream cleanup and disposed/case/run guards reject old events without requiring a full-page reload. Qualification correctly covers 11 supported pathway/depth DAGs and a distinct Deep Research/Screen refusal, not an invented twelfth DAG.

### Tasks 5–6: structured module projections and browser charts

Host projections remain outside canonical provider envelopes, bound to validated artifact identity/digest and an explicit mapping version. The complete table ledger and exact retained tables cover unsupported, missing, or malformed mappings with typed fallback reasons. Large registers are retained in bounded chunks rather than silently truncated. The first-open and chart paths introduce no analytical provider request.

Chart admission is closed and bounded: only supported recipe kinds and finite coordinates are admitted; units, period kinds, chronology, basis, perimeter, zero/negative/missing distinctions, and explicit null vocabulary are checked. Debt maturity charts use carrying value with precise labels, not commitment or guessed seniority. Source/block pairs are not expanded into an unrelated cross-product. Unsafe or precision-incompatible data remains an exact table.

G2 is a single lazy pinned runtime. Its adapter owns rendering, resize settlement, and teardown; late failures and deferred cleanup cannot publish against a replaced instance. The corrected public-attribute resize path avoids an unhandled derived rejection. Stable recipe admission survives unrelated parent updates. Signed stacks use one global series order with independent positive/negative accumulation, and the runtime-specific negative-order adjustment affects copied render data only, not canonical tables.

### Tasks 7–8: module-first reports, historical versions, incremental authority

Six developer-owned report layouts preserve Full Credit's Credit Report default. Accepted modules supply substantive first-open report content. Required evidence, optional sections, commentary, typed origins, and unavailable reasons survive save, recovery, and freeze. Client save/recovery remains serialized and CAS-bound, with explicit adoption/request-changes handling rather than silently dropping opinions or optional choices.

Template v1, template v2, and recorded mapping versions use explicit dispatch. Historical frozen bytes are not upgraded in place; a deliberate upgrade creates a new reviewed revision. CP-6 routes remain intact and report composition does not call an analytical model.

The shared publication-model resolver validates the selected READY overlay or exact ACTIVE signed revision against the current accepted run, sources, registry, calculation/lineage identity, and nearest Full Credit ancestor. Mismatched, stale, or withdrawn authority is refused instead of silently using an older base. Incremental accepted effects and unchanged prior-base values are labelled separately from selected signed assumptions, without claiming that an incremental overlay recalculates a complete forecast. Distressed and numeric-optional research paths retain their intended contracts.

### Task 9: freeze, native exports, offline verification, filing

Freeze recomposes with the recorded versions and current validated authority, compares the reviewed sections/recipes, and binds the resulting content, model identity, opinion, and complete generated-plus-analyst citation union. Registers exceeding 500 entries retain the full union through validation and offline evidence. Background rendering consumes the frozen publication rather than live chart/provider state.

New renderer v4 has explicit historical v3 dispatch, with historical golden fixtures preserved. PDF and native XLSX use the same admitted recipe semantics, full labels, zero baselines, global signed ordering, and exact equivalent tables. Export range/precision refusal is explicit rather than a visually plausible but lossy chart. Markdown remains text/table. The shared application and standalone offline Markdown renderer regions were independently compared byte-for-byte and match.

Unchanged filing and export callers were inspected. Independent filing revalidates live accepted/model/source authority, frozen integrity, and stored export bytes; signer/freezer and approver remain distinct. Historical export retrieval validates and returns frozen bytes rather than recomputing them against current authority. The five whitespace-only lines in the new Task 9 Markdown golden are documented exact renderer output, not a silent rewrite of historical fixtures.

### Task 10: qualification and evidence integrity

The final evidence distinguishes actual disposable accepted-run journeys from route-controlled fault/presentation fixtures and seeded chart/model examples. The reviewed scripts exercise the actual v2 model/edit/revision/opinion/freeze/independent-filing journey in Chromium, Firefox, and WebKit at 1440/1024/720, plus the accepted Full Credit → accepted Earnings Update v2 overlay → freeze → independent-filing journey. Fault tests wait for actual response/request settlement where required, not merely request start. The graph matrix checks exact edge pairs as well as node geometry.

The retained gates include backend 1,001 passing tests; frontend 183 unit tests, lint, TypeScript, and build; all-three-engine smoke and explicit fault coverage; 366 accessibility scans plus loaded-chart checks; 11 exact DAGs plus explicit refusal; four independently offline-verified final packages with empty findings; and 12 receipt-bound MD/PDF/XLSX download hashes. The 262-file evidence manifest was verified by the controller and the separate fresh Task 10 reviewer. Earlier failed attempts are distinguished from final successful evidence.

I independently confirmed that the server and test trees at HEAD are identical to backend-qualified `90403`, so the retained complete backend result applies to the final implementation. I did not rerun that broad suite or independently repeat all manifest/archive checks; the fresh Task 10 review and retained records are the evidence for those executions.

## Personal visual inspection and limits

Personally viewed these ten retained images against their designated review records:

- `evidence/task10/workbench-final/{chromium,firefox,webkit}/analysis-task9-signed-stacks.png`: positive, negative, mixed, and zero-bearing stacks agree with the displayed exact tables and global series order in all three engines.
- `evidence/task10/qualification-final/webkit/model-cash-flow-720.png`: narrow worksheet cash-flow rows and forecast context remain legible within the intentionally scrollable grid.
- `evidence/task10/run-graph-lifecycle-final/run-graph-branch-merge-inspector-720.png`: narrow graph scroll viewport, selected node, served edges/status, and inspector remain coherent.
- `evidence/task10/qualification-final/chromium/report-chart-table-1024.png`: report revenue chart and exact source-linked table agree.
- `evidence/task10/reviewed-export-example/pdf-pages/page-38.png` and `evidence/task10/native-xlsx/pages/sheet-12.png`: carrying-value debt maturity chart, full labels, zero baseline, legend, and exact values agree across PDF/native spreadsheet.
- `evidence/task10/reviewed-export-example/pdf-pages/page-41.png` and `evidence/task10/native-xlsx/pages/sheet-16.png`: ratio precision refusal is visible and exact values remain available; the PDF's adjacent admitted FCF chart/table is coherent.

These are personal samples, not a claim to have repeated the controller's inspection of every one of the retained example's 46 PDF pages and 30 native XLSX sheets. Original file-byte identity, all-page/all-sheet review, complete keyboard/reduced-motion checks, and the full browser matrix rely additionally on the retained root/Task 10 evidence. No fresh browser session or native office rendering was performed by this reviewer.

## Remaining evidence limits and recommendation

The complete backend stdout is a durable transcription rather than an original tee artifact; this provenance limitation is explicit. The recurring Starlette/httpx warning is documented and nonblocking. Browser journeys use disposable accepted artifacts and fixture orchestration, not paid/live analytical providers or real cases. Historical compatibility is established by explicit versioned code paths and retained regression fixtures, not an audit of unknown external historical installations. Typed chart fallbacks, including high-precision ratios, are intentional supported behavior rather than complete chartability of every table.

These limits do not leave an unfulfilled gate in the approved plan. There is no named unresolved doubt requiring another test run or a source change. The branch is specification-complete and quality-approved for handoff at the exact reviewed HEAD, with no additional fixes requested.
