# Controller confidence review — Analyst Workbench integration

Scope: the isolated branch from `31482a7912c9e58017c2784bf098a6868978cd01`, with independently approved Tasks0–9 and the final Task10 implementation awaiting its separate reviews. This record supplements, rather than replaces, the per-task owner confidence reviews and rewrite tournaments.

Least confident about, ranked:

1. **Fixture success being mistaken for real accepted publication.** Seeded renderer examples and mocked presentations alone could conceal a broken authority path. Investigated: root read actual disposable FullCredit intake/accepted snapshot/model revision/v2 report/opinion/freeze/independent-filing records for all3engines, plus actual FullCredit→EarningsUpdate accepted-overlay publication. Root separately ran the offline verifier on all4definitive packages, and hashed all12MD/PDF/XLSX downloads against their receipts. Verdict: **fine within fixture orchestration scope**; every verifier exit0/findings[], exact ancestry and independent approver retained. Paid/live analytical quality remains explicitly untested.

2. **Financial coverage or publication identity changing beneath frontend qualification.** Investigated: complete backend gate has1001passes, with root-first fixture resolution recorded. Root independently verified unchanged `caos/server` tree `b5fa78e0ec6fdc86370fbe418291b20a4a133dea` and `caos/tests` tree `8a361cc3d7bd450c85b2965844a8040985995d63` from Task9 through Task10. Prior focused tests pin full cash-flow identities, forecast-only exact-predecessor roll-forward, raw-Decimal minimum-cash horizons, model revisions, queued-work rejection and export parity. Verdict: **fine within the recorded tests**; no engine/methodology workaround or repeated unchanged27minute gate was needed.

3. **Frozen PDF/XLSX disagreeing with charts or losing citations.** Investigated: root personally inspected all46PDFpages and all30native-LibreOffice XLSXsheets of the identified actual-v2 Chromium example; all8native charts, exact chart tables, debt legend keys, zero-inclusive score axes, model/source/revision identities and explicit precision fallbacks were checked. Root also reviewed Task9's positive/negative/mixed stack examples across browser/PDF/nativeXLSX and all3full-app browser captures. Verdict: **fine for the explicitly identified reviewed examples**, with offline packages/receipts covering definitive case-specific bytes separately. Existing ordinary-cell formatting, untagged PDF and fixture-only limits are recorded, not hidden.

4. **Late callbacks or HTTP results being mistaken for a completed race test.** Investigated: root read the actual Run callback/disposal test, exact served/displayed DAG pair proof and Report receipt effect/callers. Receipt test initially relied on route fulfillment plus a delay; the owner replaced that with the exact Request's browser-level cancellation or Response.finished waiter, installed before selection/release. Final fault suite reports the expected `requestfailed` settlement and preserves receiptB. Verdict: **confirmed evidence weakness repaired**; no production race defect found in this Task10 check. Existing model calculation, revision-head, recovery and drawer races remain asserted in full smoke/fault suites.

5. **Report paper being technically rendered but unreadable.** Investigated: root's real browser images exposed inherited dark-workspace source colors on cream paper (contrast2.315/1.949) and a154px controls-open paper lane at1024. The owner reused existing paper tokens and moved the existing narrow Report arrangement to1100px. Root read the CSS/callers and bounded regression, checked all3final JSONs and complete focused exhibits. Verdict: **two confirmed production CSS bugs repaired**; each engine now reports778px open/~499px closed at1024, correct normal/hover colors and loaded-chart axe passes. No extra layout system was introduced.

6. **Stale test fixtures hiding actual failures or motivating unsafe production guards.** Investigated: selecting synthetic frozen records without required `evidence` throws in `reportEvidenceRefs` before effects commit. Root traced the real typed contract and caller; owner corrected both historical test fixtures with `evidence: []`, not a production null-coercion bypass. V2 control disclosure and optional-commentary validity required faithful selectors and visible-save settlement. Verdict: **confirmed fixture defects repaired**, with all original authority/recovery assertions retained and failure attempts recorded.

7. **Cross-engine keyboard differences being patched as application defects.** Investigated: root reproduced WebKit Tab landing on native summary while Option-Tab reaches the exact intended Report section button. The main smoke already used that macOS convention; axe now reuses it without changing the focus assertion or OS preferences. The period/row test similarly uses explicit focus+Enter to test keyboard continuity instead of assuming a mouse click focuses native buttons. Verdict: **test-platform assumptions repaired**, no application focus override. All3full smoke reports pass; root standalone122axe scans per engine pass with their keyboard checks intact.

8. **Qualification counts or historical provenance being overstated.** Investigated: root read every axe loop and found inherited output metadata claiming125scans/12Modelchecks, while actual execution is122/9. Owner corrected output constants; older successful Chromium/Firefox logs remain unaltered with the correction clearly recorded. Original backend output was transcribed rather than captured through tee; its command, exit, timing, unchanged source trees and limitation are explicit. WebKit's first successful chart-stage image was not represented as an overall pass; root later inspected the successful retry. Verdict: **reporting inaccuracies corrected**, evidence limits retained.

9. **Completion being claimed before independent review and cleanup.** Investigated: all4capability qualification evidence is present, but Task10 and fresh whole-branch Astra reviews are still separate pending gates at the time of this record. Original checkout and owned-harness cleanup must be rechecked at final handoff. Verdict: **open process gates**, not a known product bug; the controller must close them before declaring the plan complete.

Fixed: paper source contrast,1024open Report layout, missing test evidence arrays, v2 test controls/save settlement, exact receipt request-settlement proof, platform keyboard assumptions and metadata overcount.

Verified fine: accepted-v2 publication/ancestry and frozen receipt identities; recorded financial/backend tests; reviewed export/chart/table parity; all-engine live graph/Analysis/Model/Report presentation; all-engine smoke and bounded accessibility checks. Exact results and commands live in the Task10 owner report plus the root accessibility, visual and offline-verification records.

By design: host-control fixtures do not qualify live financial analysis; no paid-provider run, real-case mutation, merge or deployment is authorized. Recorded template/renderer versions preserve historical outputs; Markdown is the text/table representation; extreme-precision charts fall back to exact tables. Existing Node/Starlette warnings remain documented.

Still open: Task10 independent review, fresh Astra whole-branch review, final repository/harness handoff checks. No unresolved confirmed production defect is being carried into those reviews.
